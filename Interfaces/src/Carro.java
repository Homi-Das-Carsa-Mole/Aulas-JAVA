
public interface Carro extends Automovel { //A fim de reaproveitar métodos em comum, uma interface pode se extender de outra, assim como, neste caso, a interface "Carro"
	//se extende da interface "Automóvel", e passa a herdar os métodos nela inseridos..
	
	public void abrirPorta();
}
